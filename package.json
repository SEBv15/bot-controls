{
  "name": "bot-controls",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "foreman": "^3.0.1",
    "serialport": "^8.0.5"
  },
  "devDependencies": {
    "electron": "^7.1.1",
    "electron-rebuild": "^1.8.6",
    "node-sass": "^7.0.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "react-toggle": "^4.1.1"
  },
  "main": "src/main.js",
  "homepage": "./",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "nf start",
    "electron": "electron .",
    "pack:win64": "electron-packager . \"Robot Controls\" --out=dist/win --platform=win32 --arch=x64 --icon=src/icon.ico",    
    "electron-dev": "set ELECTRON_START_URL=http://localhost:3000 && electron ."
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
